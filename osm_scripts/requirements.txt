# Python dependencies for the OSM name-completion pipeline (osm_scripts/).
#
# complete_name.py and build_wikidata_cache.py.

# OSM I/O (used by complete_name.py and build_wikidata_cache.py).
# >=4.0 for FileProcessor/KeyFilter, used by complete_name.py's parallel mode
osmium>=4.0

# Simplified -> Traditional (Taiwan) conversion for name:zh (OpenCC s2twp) and
# Japanese shinjitai -> Traditional kanji (OpenCC jp2t). The official C++-backed
# wheel; the pure-python opencc-python-reimplemented lacks jp2t.
opencc

# Wikidata label prefetch (build_wikidata_cache.py, online step only)
requests

# Chinese romanization for name:en
hanzi2reading

# Japanese romanization for name:en
pykakasi
janome

# Other-script romanization fallbacks (used by non-zh regions)
nepali-roman
indic-transliteration
cyrtranslit
pyewts
unidecode

# Generic any-script -> Latin transliteration for name:en fallback (better than
# unidecode for Hangul/Thai/Arabic/Greek); unidecode remains the fallback if absent
PyICU
